
var creep.harvester = {
    run : function(creep){
        
    },
    
    findTask: function(creep){
        if(creep.room.memory.spawns[0])
    }
};

module.exports.creep.harvester = creep.harvester